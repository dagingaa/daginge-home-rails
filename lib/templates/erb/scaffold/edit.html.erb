<div class="action-menu">
  <%%= link_to 'Show', @<%= singular_table_name %>, <%= key_value :confirm, "'This will cancel all changes. Are you sure?'"%>, <%= key_value :method, ":get" %>, :class => "button" %>
  <%%= link_to 'Back', <%= index_helper %>_path, <%= key_value :confirm, "'This will cancel all changes. Are you sure?'"%>, <%= key_value :method, ":get" %> , :class => "button" %>
</div>
<h1>Editing <%= singular_table_name %></h1>

<%%= render 'form' %>

